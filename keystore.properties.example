# Copy this file to keystore.properties and fill with real values.
# Do not commit keystore.properties or any keystore files.

# Path can be absolute or relative to AndroidClient/.
storeFile=secrets/clipcat-upload.jks
storePassword=CHANGE_ME
keyAlias=clipcat-upload
keyPassword=CHANGE_ME
